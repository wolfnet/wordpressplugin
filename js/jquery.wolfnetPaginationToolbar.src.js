
/**
 * Code for Pagination tools.
 * "Next" and "Previous" links to paginate through result set (up to 250 listings)
 */
if ( typeof jQuery != 'undefined' ) {
	( function ( $ ) {

		$.fn.wolfnetPaginationToolbar = function () {
			//code for results tools


		} /* END: function $.fn.wolfnetPaginationToolbar */
	} )( jQuery ); /* END: jQuery IIFE */
} /* END: If jQuery Exists */
